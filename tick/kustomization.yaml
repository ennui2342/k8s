apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - pv.yaml
  # influxdb-template.yaml and telegraf-template.yaml excluded — Helm-managed
  # *-values.yaml are Helm config, not k8s manifests
