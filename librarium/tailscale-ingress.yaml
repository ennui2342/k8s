apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: librarium-ts
  namespace: librarium
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: librarium-web
      port:
        number: 3000
  tls:
    - hosts:
        - librarium
