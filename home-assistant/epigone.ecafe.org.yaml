apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: epigone.ecafe.org-production
  namespace: home-assistant
spec:
  secretName: epigone.ecafe.org-production
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - epigone.ecafe.org
