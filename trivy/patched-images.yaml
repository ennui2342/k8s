# Tracks every workload currently running a custom-forked image instead of a
# plain upstream tag, so the CVE scanner can:
#   1. suppress duplicate per-CVE nagging for images already known to be forked
#   2. periodically re-scan the *upstream* image and detect when maintainers
#      ship an equivalent fix, so we can revert instead of re-patching forever
#
# strategy: frozen-rebuild — a one-off custom image built with `docker build`
#   locally and imported into node containerd (imagePullPolicy: Never). Has a
#   Dockerfile under trivy/patched-images/<name>/. Reverting means switching
#   the manifest back to `upstream_image` and deleting the Dockerfile + this
#   entry.
# strategy: live-upgrade — the manifest runs a plain upstream image but
#   upgrades OS packages live at every pod/container start. No separate
#   patched_image or Dockerfile. Reverting means removing the live-upgrade
#   command and going back to the image's normal entrypoint, once upstream no
#   longer needs it. No current workload uses this strategy — health-monitor
#   was the last one, migrated to the shared k8s-toolbox frozen-tag image
#   (127.0.0.1:30500/k8s-toolbox, see CLAUDE.md's "Monitoring & Alerting")
#   2026-07-27, which is scanned/rebuilt directly and isn't tracked here.
# strategy: deferred-migration — the base OS is EOL (no CVE fix will ever
#   ship); the real fix is an application-level version migration, not a CVE
#   patch. `patched_image` is set to the plain image ref currently deployed
#   (so the suppression match works the same way), `upstream_image` is null
#   (nothing to reconcile against — there's no upstream fix to wait for).
#   See the linked tm task for the actual migration project.
#
# critical/high are the CRITICAL+HIGH counts resolved at patch time — the
# reconciliation scan compares the *current* upstream scan against these as
# the bar upstream needs to clear (equal or better) before we revert.

- workload: default/Deployment/mosquitto
  strategy: frozen-rebuild
  patched_image: ghcr.io/ennui2342/eclipse-mosquitto:2.1.2-patched
  upstream_image: eclipse-mosquitto:2.1.2-alpine
  build_recipe: trivy/patched-images/mosquitto/Dockerfile
  reason: "gnutls CVEs in Alpine base; no newer 2.1.2 upstream tag"
  patched_date: "2026-05-20"
  critical: 1
  high: 4

- workload: default/Deployment/modpoll
  strategy: frozen-rebuild
  patched_image: ghcr.io/ennui2342/modpoll:1.6.0-patched
  upstream_image: topmaker/modpoll:1.6.0
  build_recipe: trivy/patched-images/modpoll/Dockerfile
  reason: "openssl/libpam CVEs in Debian 12 base; no newer modpoll upstream tag"
  patched_date: "2026-05-19"
  critical: 2
  high: 8

- workload: flux-system/Deployment/image-automation-controller
  strategy: frozen-rebuild
  patched_image: 127.0.0.1:30500/image-automation-controller:v1.2.3-patched
  upstream_image: ghcr.io/fluxcd/image-automation-controller:v1.2.3
  build_recipe: trivy/patched-images/image-automation-controller/Dockerfile
  reason: "Rebased 2026-08-04 from v1.1.2 onto v1.2.3 (openssl CVE-2026-45447 now fixed upstream). v1.2.0's image.toolkit.fluxcd.io/v1beta2 CRD removal doesn't apply here — this repo only references .../v1, and this cluster's CRD storedVersions are already v1-only. Residual: golang.org/x/text CVE-2026-56852 + a grpc HIGH (GHSA-hrxh-6v49-42gf), both Go module deps baked into the binary, not Alpine OS packages — out of scope for this apk-upgrade patch strategy, identical residual to plain upstream v1.2.3"
  patched_date: "2026-08-04"
  critical: 0
  high: 2

- workload: flux-system/Deployment/kustomize-controller
  strategy: frozen-rebuild
  patched_image: 127.0.0.1:30500/kustomize-controller:v1.9.4-patched
  upstream_image: ghcr.io/fluxcd/kustomize-controller:v1.9.4
  build_recipe: trivy/patched-images/kustomize-controller/Dockerfile
  reason: "Rebased 2026-08-05 from v1.8.4 onto v1.9.4 (gnutls/nghttp2 CVEs fixed upstream; Alpine base now scans clean at 0 CRITICAL + 0 HIGH after apk upgrade, verified via local trivy 0.70.0 scan). Changelog v1.8.4->v1.9.4 reviewed: only behavior change is v1.9.0's StrictPostBuildSubstitutions feature gate going default-on, which only affects Kustomizations using spec.postBuild.substitute — this repo has none, confirmed via repo-wide grep. Residual: golang.org/x/text CVE-2026-56852 + a grpc HIGH (GHSA-hrxh-6v49-42gf), both Go module deps baked into the binary, not Alpine OS packages — out of scope for this apk-upgrade patch strategy, same residual class as the image-automation-controller fork. patched_image migrated from the old ghcr.io/ennui2342/*-patched + imagePullPolicy: Never pattern to the local registry + IfNotPresent as part of this rebuild."
  patched_date: "2026-08-05"
  critical: 0
  high: 2

- workload: default/DaemonSet/mdns-repeater
  strategy: frozen-rebuild
  patched_image: ghcr.io/ennui2342/mdns-repeater:latest-patched
  upstream_image: monstrenyatko/mdns-repeater:latest
  build_recipe: trivy/patched-images/mdns-repeater/Dockerfile
  reason: "upstream Docker Hub inactive since 2023; Alpine 3.15 base badly outdated, patched from the still-maintained v3.15 backport branch"
  patched_date: "2026-06-13"
  critical: 5
  high: 24

- workload: home-assistant/Deployment/homeassistant
  strategy: frozen-rebuild
  patched_image: 127.0.0.1:30500/home-assistant:2026.8-patched
  upstream_image: homeassistant/home-assistant:2026.8
  build_recipe: trivy/patched-images/home-assistant/Dockerfile
  reason: "Rebased 2026-08-08 from 2026.7.4 onto 2026.8 (upstream 2026.7.4 was 2 CRITICAL + 110 HIGH at scan time; upstream 2026.8 improved to 2 CRITICAL + 96 HIGH but wasn't fully clean — 37 of those HIGH were Alpine os-pkgs, the rest lang-pkgs). apk upgrade closes the remaining Alpine-base gap (verified via local trivy 0.72.0 scan: os-pkgs layer 0 CRITICAL + 0 HIGH after this patch). Changelog 2026.7.4->2026.8 reviewed (official release-notes 'Backward-incompatible changes' section, https://www.home-assistant.io/blog/2026/08/05/release-20268/): AirNow, Gardena Bluetooth, Edifier Infrared, Ohme, Paperless-ngx, the battery_level removal on LG ThinQ/Neato/Romy/Shark IQ/SwitchBot Cloud/Template/TP-Link/Xiaomi Miio, ScreenLogic's polling-interval option, UniFi Protect's 7.1 requirement, and the Volvo On Call removal — none apply, grepped live in-pod .storage/core.config_entries has no overlap (foxess_modbus, frigate, hacs, waste_collection_schedule, tasmota, webostv, mqtt, cast, etc.). The separate 'default port' change only affects new Home Assistant OS installs per the release notes, not this plain-Docker-image deployment. custom_components (foxess_modbus 1.13.6, frigate 5.15.4, hacs 2.0.5, waste_collection_schedule 2.23.0) declare no min_ha_version pin. Residual 2 CRITICAL + 59 HIGH are Python/Go/Rust language deps baked into the image (lang-pkgs/gobinary/rustbinary trivy targets), not Alpine OS packages — out of scope for this apk-upgrade patch strategy, same residual class as the flux-controller forks."
  patched_date: "2026-08-08"
  critical: 2
  high: 59

- workload: monitoring/Deployment/telegraf
  strategy: frozen-rebuild
  patched_image: 127.0.0.1:30500/telegraf:1.39.2-patched
  upstream_image: telegraf:1.39.2
  build_recipe: trivy/patched-images/telegraf/Dockerfile
  reason: "Rebased 2026-08-06 from telegraf:1.38.3 onto telegraf:1.39.2 (gnutls/libcap/nghttp2 CVEs in the Debian bookworm base now fixed upstream; Debian 12.15 layer scans clean at 0 CRITICAL + 0 HIGH fixable after apt upgrade, verified via local trivy 0.70.0 scan with --ignore-unfixed — improvement over 1.38.3's 5 CRITICAL + 44 HIGH). Changelog 1.38.3->1.39.0->1.39.1->1.39.2 reviewed (official GitHub release notes): no breaking changes apply to this config's [agent]/outputs.influxdb(x2)/inputs.statsd/inputs.mqtt_consumer/processors.regex/inputs.snmp setup. Residual: 1 CRITICAL (github.com/rclone/rclone CVE-2026-49980, fixed 1.74.3) + 1 HIGH (google.golang.org/grpc GHSA-hrxh-6v49-42gf, fixed 1.82.1), both Go module deps baked into the telegraf binary, not Debian OS packages — out of scope for this apt-upgrade patch strategy, same residual class as the flux-controller and home-assistant forks. patched_image migrated from the old ghcr.io/ennui2342/*-patched pattern to the local registry as part of this rebuild (the Deployment manifest was already on the registry pattern; this entry's metadata was stale)."
  patched_date: "2026-08-06"
  critical: 1
  high: 1

- workload: home-assistant/Deployment/ring-mqtt
  strategy: frozen-rebuild
  patched_image: ghcr.io/ennui2342/ring-mqtt:latest-patched
  upstream_image: tsightler/ring-mqtt:latest
  build_recipe: trivy/patched-images/ring-mqtt/Dockerfile
  reason: "openssl/musl/nghttp2/zlib CVEs in Alpine base"
  patched_date: "2026-05-22"
  critical: 1
  high: 7

- workload: monitoring/CronJob/cve-scanner
  strategy: frozen-rebuild
  patched_image: 127.0.0.1:30500/aquasec-trivy:0.73.0-patched
  upstream_image: aquasec/trivy:0.73.0
  build_recipe: trivy/patched-images/aquasec-trivy/Dockerfile
  reason: "Rebased 2026-08-09 from aquasec/trivy:0.72.0 onto aquasec/trivy:0.73.0 (<cli.reconcile-scanner found plain 0.73.0 upstream at 0 CRITICAL + 1 HIGH at filing time, better than 0.72.0's 0 CRITICAL + 13 HIGH but not fully clean). Changelog 0.72.0->0.73.0 reviewed (official CHANGELOG.md, https://github.com/aquasecurity/trivy/compare/v0.72.0...v0.73.0): no BREAKING CHANGES section at all for 0.73.0 (unlike 0.72.0's docker-config publishing migration) — only feature/bugfix commits (VEX OCI discovery, Java/dotnet/nodejs/conda parser fixes). No CLI flag deprecations/removals in range; every flag this CronJob passes (trivy image/kubernetes --severity/--ignore-unfixed/--format/--output/--quiet/--timeout/--parallel) is untouched. After apk upgrade, rebuilt image self-scans at 0 CRITICAL + 2 HIGH (verified via local `trivy image` scan with --ignore-unfixed against today's vulnerability DB, 2026-08-09) — the Alpine 3.24.1 os-pkgs layer itself is already 0 CRITICAL + 0 HIGH on *both* the patched build and plain upstream 0.73.0 (re-scanned both for comparison), so 0.73.0 shipped a clean Alpine base upstream and the apk-upgrade step is now a no-op for this tag; kept anyway since it's free and self-healing if that regresses on a future point release. Residual 2 HIGH (github.com/go-git/go-git/v5 CVE-2026-71556, oras.land/oras-go/v2 CVE-2026-50163) are Go module deps baked into the trivy binary, not Alpine OS packages — out of scope for this apk-upgrade patch strategy, same residual class as the flux-controller/telegraf forks. Note: these counts differ from the CVE-2026-56852/GHSA-hrxh-6v49-42gf/CVE-2026-50151/CVE-2026-39822 set recorded in the prior (0.72.0) entry — expected DB drift over the 3 days between scans, not a regression; oras-go's CVE-2026-50163 persists across both."
  patched_date: "2026-08-09"
  critical: 0
  high: 2

- workload: default/StatefulSet/web
  strategy: deferred-migration
  patched_image: "php:7.0-fpm"
  upstream_image: null
  build_recipe: null
  reason: "PHP 7.0 / Debian 9 (stretch) EOL since 2020, 28 CRITICAL + 240 HIGH unpatchable CVEs — needs a PHP 8.x migration, not weekly CVE nagging. See tm task 2f2f1b21."
  patched_date: null
  critical: 0
  high: 0

- workload: linkding/Deployment/linkding
  strategy: frozen-rebuild
  patched_image: 127.0.0.1:30500/linkding:1.46.0-patched
  upstream_image: sissbruecker/linkding:1.46.0
  build_recipe: trivy/patched-images/linkding/Dockerfile
  reason: "Rebased 2026-08-11 from sissbruecker/linkding:1.45.0 onto sissbruecker/linkding:1.46.0 (plain 1.45.0 was 10 CRITICAL + 521 HIGH; plain 1.46.0 improved to 0 CRITICAL + 1 HIGH but wasn't fully clean — residual was libcap2 CVE-2026-4878 in the Debian 13.1 os-pkgs layer). Changelog 1.45.0->1.46.0 reviewed (CHANGELOG.md + GitHub compare v1.45.0...v1.46.0): no breaking changes, only new features (bundle filtering, tag deletion API, sort-by-modified, RSS feed filtering, session duration config) and bug/dependency fixes — nothing touches this deployment's LD_CSRF_TRUSTED_ORIGINS/LD_SUPERUSER_* env vars. apt dist-upgrade against the 1.46.0 base resolves the residual libcap2 CVE — rebuilt image scans clean at 0 CRITICAL + 0 HIGH fixable (verified via local trivy 0.73.0 scan with --ignore-unfixed, both os-pkgs and python-pkgs layers)"
  patched_date: "2026-08-11"
  critical: 0
  high: 0

- workload: botkube/Deployment/botkube
  strategy: frozen-rebuild
  patched_image: ghcr.io/ennui2342/botkube:v1.14.0-patched
  upstream_image: ghcr.io/kubeshop/botkube:v1.14.0
  build_recipe: trivy/patched-images/botkube/Dockerfile
  reason: "git/musl CVEs in Alpine 3.18.9 base; botkube upstream inactive since v1.14.0 (2024-11-13), no newer release exists"
  patched_date: "2026-07-25"
  critical: 0
  high: 2

- workload: monitoring/StatefulSet/influxdb
  strategy: deferred-migration
  patched_image: "influxdb:1.8.0"
  upstream_image: null
  build_recipe: null
  reason: "Debian 9 (stretch) EOL since 2020, 5 CRITICAL + 51 HIGH unpatchable CVEs — needs an InfluxDB 1.x/2.x migration, not weekly CVE nagging. See tm task 94978139."
  patched_date: null
  critical: 0
  high: 0

- workload: freshrss/Deployment/freshrss
  strategy: frozen-rebuild
  patched_image: 127.0.0.1:30500/freshrss:1.29.1-patched
  upstream_image: freshrss/freshrss:1.29.1
  build_recipe: trivy/patched-images/freshrss/Dockerfile
  reason: "apache2/php8.4/libcurl4t64/libexpat1 (and 15 more) CVEs in Debian 13.5 (trixie) base; no newer 1.29.1-line upstream tag exists (Docker Hub last rebuilt 2026-05-20, digest unchanged since, and 1.29.1 is still the latest FreshRSS release upstream). Resolves all fixable CVEs found at patch time via trixie-security dist-upgrade — verified via local trivy 0.70.0 scan: 0 CRITICAL + 0 HIGH after patch (Debian layer upgraded to 13.6). FreshRSS app version unchanged, pure OS-package patch-level bump, no breaking changes."
  patched_date: "2026-08-07"
  critical: 2
  high: 17

- workload: wallabag/Deployment/wallabag
  strategy: frozen-rebuild
  patched_image: 127.0.0.1:30500/wallabag:2.6.14-patched
  upstream_image: wallabag/wallabag:2.6.14
  build_recipe: trivy/patched-images/wallabag/Dockerfile
  reason: "musl/php81 CVEs in Alpine 3.19.8 base (php 8.1.32-r0 -> 8.1.34-r0, musl 1.2.4_git20230717-r5 -> r6); no newer 2.6.14-line upstream tag exists (Docker Hub last published 2025-10-07)"
  patched_date: "2026-08-01"
  critical: 2
  high: 9

- workload: monitoring/Deployment/kube-prometheus-stack-grafana
  strategy: frozen-rebuild
  patched_image: 127.0.0.1:30500/grafana:11.6.16-patched
  upstream_image: grafana/grafana:11.6.16
  build_recipe: trivy/patched-images/grafana/Dockerfile
  reason: "c-ares/curl CVEs (9 HIGH: CVE-2026-33630, CVE-2026-11352, CVE-2026-11586, CVE-2026-12064, CVE-2026-8286, CVE-2026-8458, CVE-2026-8925, CVE-2026-8927, CVE-2026-9547) in Alpine 3.24.1 base; 11.6.16 is the newest tag on the 11.6.x line (upstream has moved to 12.x/13.x, a major-version jump out of scope for a routine CVE pass — see tm task for the tracked follow-up). Fixes for both packages were already published in the same Alpine 3.24 repos (c-ares 1.34.8-r0, curl/libcurl 8.21.0-r0), so a same-base apk upgrade resolves all 9 without any grafana version change. Verified via local trivy 0.73.0 scan: alpine/os-pkgs layer 0 CRITICAL + 0 HIGH after patch."
  patched_date: "2026-08-12"
  critical: 0
  high: 9
