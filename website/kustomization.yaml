apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
resources:
  - configmap.yaml
  - ingress.yaml
  - nginx.yaml
  - phpconfigmap.yaml
  - pv.yaml
  - svc.yaml
  # php.ini excluded — referenced by phpconfigmap.yaml but not a k8s manifest itself
